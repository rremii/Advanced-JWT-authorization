// PAGE SETTINGS ===============================================================================================================================================================================================================================================================================================
// minimum width of page
export let minWidth = 320

// maximum width of page
export let maxWidth = 1980

// width of bounding container(0 = no padding)
export let maxWidthWrapper = 1980

// general padding of container
// (30 =  15px to left and right, 0 = no padding)
export let wrapperPadding = 30;

// The width of the first breakpoint trigger
export let wrapperWidth = maxWidthWrapper + wrapperPadding;
// ================================================================================================================================================
//break points (can be changed) use for media queries
export let ultraWide = `1981px`
export let desctop = `1980px`
export let tablet = `990px`
export let mobile = `768px`
export let mobileSmall = `480px`


